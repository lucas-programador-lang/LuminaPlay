// API configuration and constants

const API_CONFIG = {
    baseURL: 'https://api.example.com',
    timeout: 5000,
};

const ENDPOINTS = {
    getUser: '/users',
    getPosts: '/posts',
};

export { API_CONFIG, ENDPOINTS };